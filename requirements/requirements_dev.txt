build
django-debug-toolbar==6.0.0
django-test-migrations
drf-spectacular>=0.27.0  # Modern OpenAPI 3.0 schema generator
# pprofile - re-add once https://github.com/vpelletier/pprofile/issues/41 is addressed
ipython>=7.31.1 # https://github.com/ansible/awx/security/dependabot/30
unittest2
black
pytest!=7.0.0
pytest-asyncio
pytest-cov
pytest-django
pytest-mock==1.11.1
pytest-timeout
pytest-xdist
tox  # for awxkit
logutils
jupyter
notebook<7.0.0
# matplotlib - Caused issues when bumping to setuptools 58
backports.tempfile  # support in unit tests for py32+ tempfile.TemporaryDirectory
# artefactual-labs fork pinned to a commit (py3 fixes; the PyPI release is
# abandoned). Fetched as a tarball so the download goes through pip (bundled
# CA store, automatic retries) rather than a git subprocess, which proved
# flaky in CI image builds.
https://github.com/artefactual-labs/mockldap/archive/61b362ef6a45911e2ec33923da3af17d51157dac.tar.gz#egg=mockldap
gprof2dot
atomicwrites==1.4.0
flake8
yamllint
pip>=26.1 # PEP 660 – Editable installs for pyproject.toml based builds (wheel based)

# python debuggers
debugpy
remote-pdb
sdb
